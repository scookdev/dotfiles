os: osx
osx_image: xcode8
# Change SSH submodules to HTTP per https://goo.gl/064FGx
before_install:
  - sed -ie 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive
script: script/cibuild
env:
  SHELL: zsh
git:
  submodules: false
